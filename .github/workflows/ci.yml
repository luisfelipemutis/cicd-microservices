name: CI - Build and Test

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

env:
  ACR_NAME: acr20251119201901
  RESOURCE_GROUP: rg-cicd-mexico

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Test API structure
      run: |
        cd api
        ls -la
        [ -f "app.py" ] && echo "✅ API app.py found" || exit 1
        [ -f "Dockerfile" ] && echo "✅ API Dockerfile found" || exit 1
        [ -f "requirements.txt" ] && echo "✅ API requirements.txt found" || exit 1
    
    - name: Test Worker structure  
      run: |
        cd worker
        ls -la
        [ -f "worker.py" ] && echo "✅ Worker worker.py found" || exit 1
        [ -f "Dockerfile" ] && echo "✅ Worker Dockerfile found" || exit 1

  build-and-push:
    needs: test
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Azure Login
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Build and push API image
      run: |
        echo "📦 Building API image..."
        az acr build \
          --registry ${{ env.ACR_NAME }} \
          --image api:${{ github.sha }} \
          --image api:latest \
          --file api/Dockerfile \
          ./api/ \
          --no-logs

    - name: Build and push Worker image
      run: |
        echo "🏭 Building Worker image..."
        az acr build \
          --registry ${{ env.ACR_NAME }} \
          --image worker:${{ github.sha }} \
          --image worker:latest \
          --file worker/Dockerfile \
          ./worker/ \
          --no-logs

    - name: Verify images
      run: |
        echo "✅ Verifying images in ACR..."
        az acr repository list --name ${{ env.ACR_NAME }} --output table
        echo "--- API tags ---"
        az acr repository show-tags --name ${{ env.ACR_NAME }} --repository api --output table
        echo "--- Worker tags ---"
        az acr repository show-tags --name ${{ env.ACR_NAME }} --repository worker --output table
